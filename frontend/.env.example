# API Configuration
NEXT_PUBLIC_API_URL=https://thesimpleai.vercel.app

# App Configuration
NEXT_PUBLIC_APP_NAME=Enterprise AI Hub
NEXT_PUBLIC_COMPANY_DOMAIN=securemaxtech.com

# Feature Flags
# Set to 'true' to use new Prisma-based ticketing system (Phase 2+)
# Set to 'false' or leave unset to use legacy backend ticketing system
NEXT_PUBLIC_USE_NEW_TICKETING=false

# Prisma Configuration (Required for Next.js API routes)
# Use the NON-POOLING connection string from Neon for read-after-write consistency
# Find this in Neon Console -> Connection Details -> "Direct connection" (not "Pooled connection")
POSTGRES_URL_NON_POOLING=postgresql://username:password@ep-xxx.region.neon.tech/database?sslmode=require

# JWT Configuration (must match backend values)
JWT_SECRET=your_super_secure_jwt_secret_at_least_32_characters_long

# Development Override (uncomment for local development)
# NEXT_PUBLIC_API_URL=http://localhost:5000/api