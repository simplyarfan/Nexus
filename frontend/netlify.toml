[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# NOTE: Backend API proxying disabled - configure NEXT_PUBLIC_API_URL environment variable instead
# If you need to proxy API requests through Netlify, uncomment and update the redirects below
# with your backend API URL (e.g., https://your-backend.vercel.app)

# [[redirects]]
#   from = "/api/auth/*"
#   to = "https://your-backend-api.vercel.app/api/auth/:splat"
#   status = 200

# [[redirects]]
#   from = "/api/analytics/*"
#   to = "https://your-backend-api.vercel.app/api/analytics/:splat"
#   status = 200

# [[redirects]]
#   from = "/api/cv-intelligence/*"
#   to = "https://your-backend-api.vercel.app/api/cv-intelligence/:splat"
#   status = 200

# [[redirects]]
#   from = "/api/notifications/*"
#   to = "https://your-backend-api.vercel.app/api/notifications/:splat"
#   status = 200

# [[redirects]]
#   from = "/api/interview-coordinator/*"
#   to = "https://your-backend-api.vercel.app/api/interview-coordinator/:splat"
#   status = 200

# [[redirects]]
#   from = "/api/init/*"
#   to = "https://your-backend-api.vercel.app/api/init/:splat"
#   status = 200

# [[redirects]]
#   from = "/api/tickets/*"
#   to = "https://your-backend-api.vercel.app/api/tickets/:splat"
#   status = 200

# SPA fallback for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Security headers
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    X-XSS-Protection = "1; mode=block"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Cross-Origin-Opener-Policy = "unsafe-none"
    Cross-Origin-Embedder-Policy = "unsafe-none"
